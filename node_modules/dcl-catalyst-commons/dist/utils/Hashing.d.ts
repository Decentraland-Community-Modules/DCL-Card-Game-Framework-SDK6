/// <reference types="node" />
import { ContentFileHash, ContentFile } from '../types';
export declare class Hashing {
    /** Given a set of files, return a map with their hash */
    static calculateHashes(files: ContentFile[]): Promise<{
        hash: ContentFileHash;
        file: ContentFile;
    }[]>;
    /** Return the given file's hash */
    static calculateHash(file: ContentFile): Promise<ContentFileHash>;
    /** Return the given buffer's hash */
    static calculateBufferHash(buffer: Buffer): Promise<ContentFileHash>;
}
